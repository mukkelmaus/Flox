# OneTask API Environment Variables - Example Configuration
# Copy this file to .env and update the values accordingly

# Security Settings
SECRET_KEY=your_jwt_secret_key_here           # JWT secret key
SESSION_SECRET=your_session_secret_key_here   # Flask session secret key

# Database Settings
DATABASE_URL=postgresql://postgres:password@localhost:5432/onetask  # PostgreSQL connection URL

# API Settings
API_V1_STR=/api/v1            # API version prefix
PROJECT_NAME=OneTask API      # Project name
VERSION=1.0.0                 # API version
DESCRIPTION=AI-powered task management API for neurodivergent users

# App Settings
ENVIRONMENT=development       # development or production
SERVER_HOST=http://localhost:5000  # Server host URL
LOG_LEVEL=debug               # Logging level (debug, info, warning, error)

# Worker Settings (for production)
WORKERS=1                     # Number of Gunicorn workers
WORKER_TIMEOUT=180            # Worker timeout in seconds
KEEP_ALIVE=5                  # Keep-alive timeout in seconds

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000  # Comma-separated list of allowed origins

# JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=60  # Token expiration time in minutes
ALGORITHM=HS256                 # JWT algorithm

# OpenAI Settings (for AI features)
OPENAI_API_KEY=your_openai_api_key_here  # Required for AI features