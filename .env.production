# OneTask API Environment Variables - PRODUCTION CONFIGURATION
# Security Settings
SECRET_KEY=onetask_secure_jwt_secret_key_for_production
SESSION_SECRET=onetask_secure_session_secret_key

# Database Settings
# PostgreSQL database URL is set at runtime via Replit DATABASE_URL

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=OneTask API
VERSION=1.0.0
DESCRIPTION=AI-powered task management API for neurodivergent users

# App Settings
ENVIRONMENT=production
SERVER_HOST=https://onetask.replit.app
LOG_LEVEL=info

# Worker Settings
WORKERS=1
WORKER_TIMEOUT=180
KEEP_ALIVE=5

# CORS Settings
ALLOWED_ORIGINS=https://onetask.replit.app,https://*.replit.app

# JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256

# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# The following settings should be set via environment variables in production:
# OPENAI_API_KEY - Add this via secrets management if using AI features